# Solana Radar V1.1

A research/paper-analysis radar for Solana tokens, with meme-coin discovery, market scoring, historical observations and a mobile dashboard. No trading execution or custody.

## Run

```bash
python -m venv .venv
. .venv/bin/activate
pip install -r requirements.txt
python -m pytest -q
./run.sh
