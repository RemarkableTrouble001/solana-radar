[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "solana-radar"
version = "1.1.0"
description = "Research and paper-analysis radar for Solana tokens"
requires-python = ">=3.11"
